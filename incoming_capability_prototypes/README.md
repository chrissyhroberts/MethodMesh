# Historical capability prototypes and patch handoffs

This directory preserves source packages, successive experiments and installation handoffs for review and reproducibility. It is included in GitHub intentionally. Contents may duplicate older implementations or contain superseded Master Books; they are not compiled and are not current architectural authorities.

Current implementation: `app/src/main/java/com/example/methodmesh/`.
Current project authority: [Master Book v1.25](../docs/METHODMESH_MASTER_BOOK.md).

Local rollback copies and generated build outputs remain excluded from Git. Review an installer before applying an old handoff to the current app.
