---
title: "GPS target navigation"
---

::: {.status-box .status-complete}
**Status: Documented and demonstrated**

This status is based on the supplied project documentation and example XLSForms. It is not a formal validation claim.
:::


## Purpose

Guide a user toward target coordinates using distance, bearing and an optional augmented-reality camera overlay.

## Capability IDs

- `gps_target_navigator`

## Requirements

- Location permission.
- Camera permission when AR mode is enabled.

## Screenshot

::: {.screenshot-placeholder}
**Screenshot placeholder**

Add a current capability dashboard or execution screenshot at:

`assets/screenshots/gps-target-navigator.png`
:::

<!-- Replace the placeholder above with:
![](../assets/screenshots/gps-target-navigator.png){fig-alt="GPS target navigation screen in MethodMesh"}
-->

## Supported XLSForm calls


**From `example_odk_gps_target_navigator.xlsx`:**

```text
com.example.methodmesh.EXECUTE_METHOD(method_id='gps_target_navigator',input_target_name=${target_name_input},input_target_plus_code=${target_plus_code_input},input_target_latitude=${target_latitude_input},input_target_longitude=${target_longitude_input},input_arrival_radius_m=${arrival_radius_input},input_payload_mode='FULL',return_mode='flat')
```


## Inputs observed in supplied examples

| Field | Label | XLSForm type |
|---|---|---|
| destination_mode | Destination format | select_one destination_mode |
| target_name_input | Target name | text |
| target_plus_code_input | Target Plus Code | text |
| target_latitude_input | Target latitude | decimal |
| target_longitude_input | Target longitude | decimal |
| arrival_radius_input | Arrival radius (metres) | decimal |


## Expected returned fields

Return fields are documented from the specific XLSForm execution group only.

The previous generated documentation layer contained duplicated fields across capabilities. This section has been regenerated and will only include verified fields from the matching execution group.

The supplied ODK example returns the navigation values plus
`methodmesh_full_json` for complete metadata/audit storage.
