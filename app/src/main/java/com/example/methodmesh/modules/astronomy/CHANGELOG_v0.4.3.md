# Astronomy v0.4.3

- Native preset runs of `astronomy.dashboard` now remain on the live visual dashboard after refresh instead of transitioning to the generic result page.
- Native preset dashboard confirmation is labelled `Finish`; normal dashboard use retains `Use this snapshot`.
- External / ODK invocations are unchanged and still return structured results automatically.
