# Weather v0.1.1

- Replaced `Modifier.matchParentSize()` in `WeatherRadarMap.kt` with `Modifier.fillMaxSize()` for compatibility with the current MethodMesh Compose dependency set.
- Updated validation notes accordingly.
- No capability IDs, settings, outputs, ODK contracts, provider semantics or XLSForms changed.
