# Sampling prototype review — 2026-09-03

## Status

Initial Work-mode review started.

Sampling is a plausible MethodMesh capability prototype and is much closer to the capability-writing guide than the other incoming prototype.

## Conformity check

- Canonical single-folder delivery: pass.
- Nested docs folder: pass.
- Example ODK/XLSForm present: pass.
- ODK intent is on a `begin_group`: pass.
- Starts as Development: pass.
- Declares settings through `capabilitySettings()`: pass.
- Uses choices/checkboxes for fixed enumerations: mostly pass.
- Uses `CapabilityScreenScaffold`: pass.
- Attempts fixed-preset hiding through `settingShouldBeShown`: pass.
- Beef-first outputs plus audit JSON: pass.
- Avoids shared dashboard special-casing: pass.

## Fix applied during review

`SamplingFiles` exposed `internal` return types from public functions, which prevented Kotlin compilation. `SamplingCsvInput` and `SamplingFileArtifacts` were made public data classes.

## Build verification

The prototype was temporarily copied into a clean disposable worktree at:

```text
/private/tmp/methodmesh_sampling_review/app/src/main/java/com/example/methodmesh/modules/sampling
```

Verification command:

```text
./gradlew :app:compileDebugKotlin
```

Result: passed.

The disposable worktree was removed after verification.

## Remaining review before admission

- Native UX needs device review; the screen is powerful but may be too busy for normal field use.
- Native share behaviour for generic CSV/JSON attachments depends on shared result helpers and may need framework support.
- Preset runtime should be checked on device for each source type, especially CSV and structured JSON.
- ODK example should be run through Collect/Kobo, not only workbook-inspected.
- Decide whether `DROP_IN.md` and `PACKAGE_CONTENTS.txt` should remain as docs or be removed before admission.

## Recommendation

Keep in `incoming_capability_prototypes/sampling` for now. It is buildable after the small visibility fix, but should not be promoted to the real modules folder until native/preset/ODK behaviour has been exercised.
