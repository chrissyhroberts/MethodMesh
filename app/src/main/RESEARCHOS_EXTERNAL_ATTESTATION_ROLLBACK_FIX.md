# External attestation transport correction

Capability-specific intent parameters remain in workflow/action settings. Only core caller/entity fields are projected into InvocationContext. The attestation screen reads settings first and overlays canonical invocation identifiers, so external values are retained without being lost by the narrower InvocationContext model.
